# Предложения изменений документации

Формат: дата · файл(ы) · что изменить · почему · влияние на код и тесты. Владелец отмечает: принято / отклонено.


## 2026-09-24 · Дороги пунктиром, не «метро» · пожелание владельца, ждёт решения по конфликтам

- **Что:** дороги рисуются пунктирной линией, а не толстой линией «метро», и не поверх всего (ниже фишек, городов, фронта и узоров?). Города на карту пока не добавляем.
- **Файлы:** `art/style-guide.md` (идея, принцип 3, слой 7, раздел «Линии снабжения», «Изолированная сеть», «Стройка»), `art/tokens.json` (`road.*`), `art/ui.md` (строки 33–34: «линия-метро» / «пунктирная линия» в карточке города), `art/units.md` (стрелка — «как поток снабжения»), `gdd/04-roads-supply.md` (строки 3, 20, 57, 63), `01-vision.md` (столп 1), `DECISIONS.md` 2026-09-23 «Визуальный стиль „метро-карта“», `stages/stage-04` T4.
- **Конфликты, которые надо решить до правки:**
  1. Пунктир уже занят: изолированная сеть (7/7), стройка дороги («муравьи»), путь «Перестроить снабжение», изолированный город, контур котла. Если все дороги пунктирные, чем показать разрез/изоляцию — цветом, прозрачностью, значком разрыва?
  2. Поток снабжения (белый бегущий пунктир поверх линии) на пунктирной дороге не читается. Оставить поток, заменить (бегущие точки/стрелки) или убрать?
  3. Столп «Логистика решает войну» и принцип «Снабжение — главный герой, самая толстая линия после армий»: дорога становится второстепенным элементом. Меняем приоритет или только форму линии?
  4. Слой 7 — между территорией и городами. Куда именно опустить дороги: под узоры рельефа (4а), под границы (5)?
- **Влияние на код:** этап 01 — нет (дорог в игровом коде нет; пример дороги удалён с `/dev/map`). Этап 04 T4 — реализация по новой спецификации.

## 2026-09-24 · Синие цвета игроков похожи на воду · предложение агента

- **Что:** проверить ΔE2000 заливок игроков (`mix(line, #FFF, fillMix)` при `alphaOther`/`alphaOwn` поверх равнины) против `map.water`; цвета ниже порога — сдвинуть по оттенку или исключить из соседства с водой.
- **Почему:** на скриншотах `cobalt` (южное государство) читается как озеро; то же ожидается у `ocean`, `cyan`, `navy`, `steel`, `indigo`.
- **Файлы:** `art/tokens.json` (`players.palette`), тест в `apps/client/test/terrain-palettes.test.ts`.
- **Влияние:** только клиент; эталоны скриншотов.
