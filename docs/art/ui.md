# UI: HUD, карточки, панели

Все значения — из `tokens.json`. Раскладка проверяется скриншотами на 390×844 (телефон, портрет), 844×390 (телефон, альбом), 1440×900 (ПК).

## HUD (верх)

Белая полоса высотой 44 (телефон) / 48 (ПК), `shadow.hard` снизу, без скругления по краям экрана.

| Слот | Содержимое | Формат |
| --- | --- | --- |
| Люди | подпись 12 px `ink2` + значение Unbounded 15 + прирост 12 px `ink2` | `12 400  +38/с` |
| Золото | то же; прирост красный, если отрицательный | `1 820  +12/с` |
| Налог | компактный ползунок 0–40 % с делениями по 5 %, значение справа; при тапе раскрывается подсказка «рост ×1,0 · доход +1,1/с» | `20 %` |
| Снабжение | процент основной сети; < 100 % — `status.warning`, < 50 % — `status.danger` | `94 %` |
| Таймер | Unbounded 15 | `07:42` |
| Место | `#4/30` | |

На телефоне в портрете — две строки: ресурсы + налог сверху, снабжение/таймер/место справа вторым рядом мельче.

## Карточка выбранного (снизу слева)

- Ширина 320 (ПК) / вся ширина минус 16 px поля (телефон, выезжает снизу как лист).
- Фон `ui.surface`, радиус 12, обводка 1 px `ui.border`, `shadow.hard`.
- Шапка: название (Golos 500, 16), под ним статус 12 px.
- Строки параметров: подпись `ink2` слева, значение справа, tabular-nums.
- Кнопки: высота 44, радиус 8. Главная — `buttonPrimaryBg`, остальные — белые с обводкой `borderStrong`. Цена на кнопке справа приглушённо: «Улучшить · 200».
- Показываются только действия, возможные для выбранного объекта. Нейтральный или чужой гекс — только информация, без кнопок. Действие, которое сейчас недоступно по правилам (дистанция до города, максимальный уровень, нет дороги для склада и т. п.), — скрыто. Исключение — **«Основать город»**: на своём гексе без города кнопка видна всегда, недоступная — приглушённая, с причиной строкой под ней («Ближе 4 гексов к городу», «Население 34 % из 60 %»), чтобы было понятно, как к городу прийти. `[ДИЗАЙН-РЕШЕНИЕ 2026-09-25]`
- Действие, которому не хватает только золота, показывается с ценой; по тапу — причина строкой под кнопкой (`status.danger`, 12 px). Постоянных красных ошибок в карточке нет.
- Идущая стройка на гексе — строка «Строится: <что> · <осталось> с» с полосой прогресса; на карте — дуга прогресса вокруг гекса.

### Карточка города — статус снабжения

| Состояние | Строка статуса |
| --- | --- |
| В основной сети | `Связан со столицей` — `status.success`, маленькая линия-«метро» цвета игрока слева |
| Изолирован | `Изолирован · доход −50 %` — `status.danger`, пунктирная линия слева; ниже кнопка **«Перестроить снабжение · 90»** |
| Перестройка идёт | прогресс-полоса + «Прокладка дороги · 6 из 9» |
| Нет пути | Кнопка остаётся, по тапу: «Город окружён — нет пути по своей земле» |

При нажатии/наведении на «Перестроить снабжение» на карте пунктиром подсвечивается будущий путь.

## Панель армий (CR-001, CR-003 — как в HoI4)

Состав и кнопки — `gdd/07-controls.md`, «Панель армий». Стиль — как у карточки выбранного: фон `ui.surface`, радиус 12, обводка 1 px `ui.border`, `shadow.hard`.

- Ряд карточек армий внизу экрана, горизонтальная прокрутка; карточка 176 px (телефон 150): полоса цвета армии 8 px во всю высоту, название (Golos 500, 13), солдаты tabular-nums, полоски «Орг.» (`status.success`) и «Снабж.» (цвет как столбик снабжения фишки, `units.md`), строка 12 px `ink2`: «N отр. · план» или «Истощение: N отр.» (`status.danger`). Выбранная карточка — обводка 2 px `ui.ink`.
- Над рядом, слева (над карточкой) — приказы выбранной армии: кнопки 64×48, иконка 20×20 (линия 2, round) и подпись 12 px; включённый инструмент — обводка `ui.ink` и фон `ui.surface2`, под кнопками — подсказка 12 px `ink2` одной строкой; на телефоне кнопки переносятся (CR-004).
- Последняя карточка: «Резерв: N отр.» (выбрать отряды резерва), «Автопополнение», «+ Новая армия».
- Карточки отряда и гекса встают над рядом армий; при включённом инструменте карточки скрыты. Плашек с кнопками подтверждения нет (CR-004).
- Переключатель вида фишки и легенда (свои/враги, полоса армии, организация, снабжение, треугольник) — слева под HUD, пока владелец не выберет вариант.
- Итоговая визуальная приёмка — этап 04/T8.

## Кнопка действия (снизу справа)

Круглая 56 (телефон) / пилюля с текстом (ПК): «Наступление», «На фронт», «Экспансия». Unbounded 500, 12 px на ПК. Фон — цвет игрока `line`, текст белый.

## Лента событий

Слева под HUD, до 3 строк, каждая исчезает через 6 с. Строка: цветная точка-владелец + текст 13 px. Тап — камера к месту события. Важные события (котёл, потеря города, столица) — с полосой слева `status.danger` 3 px, радиус 0.

## Меню и лобби

- Фон — анимированная (медленно) карта-демо с ботами за полупрозрачной белой панелью (`surface` 92 %, без blur).
- Вордмарк сверху, кнопка «Играть» — единственная тёмная кнопка экрана.
- Никаких карточек-«плиток» с иконками режимов на MVP.

## Иконки UI

Tabler Icons (outline, stroke 1.75), только для кнопок интерфейса (настройки, закрыть, звук). На карте — только свои глифы из `units.md`.
