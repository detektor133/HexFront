# Статус

Обновляется агентом в конце каждой сессии. Читается в начале каждой сессии.

| Поле | Значение |
| --- | --- |
| Текущий этап | 03 — Армии, снабжение армий, бой, отступление |
| Текущая задача | 03/T5 |
| Ветка | `stage-03` |
| Последнее обновление | 2026-09-25 |

## Последняя сессия

- Допущения набора 03/T1 подтверждены владельцем (`DECISIONS.md`).
- **CR-001 «Армия — группа отрядов»** (как в HoI4) принят владельцем и применён к документам: отряд — однотипная фишка на карте (до 3 в гексе, лимит 4 + 2 × городов), армия — группа отрядов с номером и именем, получает приказы, фронт и стрелки; набор — в резерв, переключатель «Автопополнение».
- 03/T13: в коде `Army` → `Unit`, `armies` → `units`, константы `MAX_UNITS_PER_HEX`, `UNIT_LIMIT_*`, `START_UNITS`; поведение не менялось.
- 03/T14: армии (`state.armies`, `unit.armyId`), команды `createArmy`, `renameArmy`, `disbandArmy`, `assignUnits`, `armyOrder`; стартовые отряды — в «1-й армии».
- 03/T15: набор в резерв, `setAutoReinforce` — армия с наименьшим числом солдат (до этапа 04).
- Golden: старт `small` — `65521470`, реплей «10 минут развития» — `c6e92e95`.
- 03/T4: `supplySystem` — производство сетей, потери пути вне дорог (склады ×0,5), приписка к лучшей сети, банкротство ×0,5, котёл; `attritionSystem` — истощение после 20 с ниже 50 %. `BANKRUPT_SUPPLY_MULT` вынесен в баланс. Golden: старт `8608a130`, реплей `b043c60f`. Два допущения — в `QUESTIONS.md`.
- Ранее в этапе: T1 (набор, содержание, банкротство), T2 (движение), T3 (захват, `expand`).

## Дальше

- Владелец: подтвердить допущения 03/T4 в `QUESTIONS.md`.
- 03/T5 — `combatSystem` (бой через организованность).
- Затем (в T5 атака при входе во врага заменит остановку) T6 (отступление), T7–T12; T16 [CR-001] — вместе с T8, T10, T12. После T16 — CR-001 в статус «применён».
- До этапа 04 — решения в `CHANGELOG-proposals.md` (синие цвета игроков).

## Блокеры и открытые вопросы

Нет. Риски — в отчёте этапа.
