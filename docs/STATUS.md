# Статус

Обновляется агентом в конце каждой сессии. Читается в начале каждой сессии.

| Поле | Значение |
| --- | --- |
| Текущий этап | 03 — Отряды и армии, снабжение, бой, отступление (на приёмке) |
| Текущая задача | — |
| Ветка | `stage-03` |
| Последнее обновление | 2026-09-25 |

## Последняя сессия

- Допущения набора 03/T1 подтверждены владельцем (`DECISIONS.md`).
- **CR-001 «Армия — группа отрядов»** (как в HoI4) принят владельцем и применён к документам: отряд — однотипная фишка на карте (до 3 в гексе, лимит 4 + 2 × городов), армия — группа отрядов с номером и именем, получает приказы, фронт и стрелки; набор — в резерв, переключатель «Автопополнение».
- 03/T13: в коде `Army` → `Unit`, `armies` → `units`, константы `MAX_UNITS_PER_HEX`, `UNIT_LIMIT_*`, `START_UNITS`; поведение не менялось.
- 03/T14: армии (`state.armies`, `unit.armyId`), команды `createArmy`, `renameArmy`, `disbandArmy`, `assignUnits`, `armyOrder`; стартовые отряды — в «1-й армии».
- 03/T15: набор в резерв, `setAutoReinforce` — армия с наименьшим числом солдат (до этапа 04).
- Golden: старт `small` — `65521470`, реплей «10 минут развития» — `c6e92e95`.
- 03/T8: `forecastBattle(map, view, unitIds, target)` по общим формулам (`queries/battle-math.ts`); `playerView` — отряды (чужое снабжение, армия, путь скрыты), свои армии, оборона городов. Приёмка: 47 из 49 решительных прогнозов (95,9 %). Допущения 03/T7 подтверждены.
- 03/T7: `artillerySystem` — обстрел в радиусе 2, автоцель, фокус `bombard`, поддержка +30 % без сложения, бегство одинокой артиллерии −10 %. Golden: старт `72fe4ea3`, реплей `b9f80fa4`. Допущения — в `QUESTIONS.md`.
- 03/T5–T6: бой (`combatSystem`: сила сторон, фланги, реки, местность, укрепления, город; ополчение всегда защищает город и восстанавливается 4/с, гарнизон — как пехота; победитель сразу входит в гекс), отступление (гекс по приоритету, −5 %, «отступает» на время хода ×0,7, двойные потери, затем org 20), капитуляция; команда `attack`; `orgRegenSystem`. Новое число баланса `MILITIA_REGEN_PER_S` = 4. Golden: старт `9c3575c3`, реплей `72815884`.
- Подтверждены владельцем: допущения 03/T4, решения по бою (`DECISIONS.md`).
- 03/T4: `supplySystem` — производство сетей, потери пути вне дорог (склады ×0,5), приписка к лучшей сети, банкротство ×0,5, котёл; `attritionSystem` — истощение после 20 с ниже 50 %. `BANKRUPT_SUPPLY_MULT` вынесен в баланс. Golden: старт `8608a130`, реплей `b043c60f`. Два допущения — в `QUESTIONS.md`.
- Ранее в этапе: T1 (набор, содержание, банкротство), T2 (движение), T3 (захват, `expand`).

## Дальше

- Владелец: принять или поправить CR-002 (`docs/changes/CR-002-front-lines.md`) — фронты, линии обороны и стрелки как в HoI4, первыми в этапе 04.
- Владелец: приёмка этапа 03 по `docs/reports/stage-03.md` (перезапустить dev-сервер: Vite кэширует старый `sim` для воркера); отправить ветку с исправленной историей сообщений: `git push --force-with-lease origin stage-03` (локально исправлено, копия — `backup/stage-03-before-msgfix`); затем PR `stage-03` → `main`, тег `stage-03-done`.
- Этап 04 — начинается с CR-002.

## Блокеры и открытые вопросы

Нет. Риски — в отчёте этапа.
