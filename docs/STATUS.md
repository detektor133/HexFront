# Статус

Обновляется агентом в конце каждой сессии. Читается в начале каждой сессии.

| Поле | Значение |
| --- | --- |
| Текущий этап | 03 — Армии, снабжение армий, бой, отступление |
| Текущая задача | 03/T4 |
| Ветка | `stage-03` |
| Последнее обновление | 2026-09-25 |

## Последняя сессия

- Этап 02 принят: PR #2 смержен, тег `stage-02-done`. Начат этап 03, ветка `stage-03`.
- 03/T1: команда `recruit` — люди из радиуса 2 города пропорционально излишку над 10 % лимита, золото сразу, одна очередь на город, лимит армий 4 + 2 × городов (с очередью), появление в ближайшем своём свободном гексе; содержание армий в `economySystem`, банкротство (`players[].bankrupt`) блокирует набор и стройки. HUD показывает баланс золота за вычетом содержания.
- 03/T2: `findPath` (A* по времени хода, ничьи по HexId, обход полных гексов), `movementSystem` (дороги по любым, реки, ЗК ×2, снабжение < 50 %), команды `move`, `setOrder`, `split`, `merge`. Решения владельца по движению — в `05-armies.md`.
- 03/T3: `captureHex` — захват по завершении перехода (у игрока −20 % населения, нейтральный без потерь), приказ `expand` — ближайший нейтральный гекс у границы, без повторных целей, без целей → `idle`.
- Golden: старт `small` — `d44454d7`, реплей «10 минут развития» — `727380fa` (записи `DECISIONS.md` 2026-09-25).
- DSL: `recruit`, `move`, `setOrder`, `split`, `merge`, `river`, `setSupply`, `setOrg`, `armyById`; `setOwner` переносит и город; нейтральные города получают гарнизон по уровню.

## Дальше

- Владелец: подтвердить допущения 03/T1 в `QUESTIONS.md`.
- 03/T4 — `supplySystem` (снабжение армий, склады, истощение, котёл; банкротство ×0,5).
- Затем T5 (бой — атака при входе во врага заменит остановку перед ним), T6 (отступление), T7–T12.
- До этапа 04 — решения в `CHANGELOG-proposals.md` (синие цвета игроков).

## Блокеры и открытые вопросы

Нет. Риски — в отчёте этапа.
