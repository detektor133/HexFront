// Зоны обзора (раз в 10 тиков).
// GDD: docs/gdd/08-match.md — «Туман войны»
import type { MatchState } from '../state/types.ts';

/** Зоны обзора (раз в 10 тиков). Пока без логики: механика появляется в своём этапе. */
export function visionSystem(_state: MatchState): void {}
