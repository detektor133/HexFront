// Сети снабжения и изоляция городов (раз в 10 тиков).
// GDD: docs/gdd/04-roads-supply.md
import type { MatchState } from '../state/types.ts';

/** Сети снабжения и изоляция городов (раз в 10 тиков). Пока без логики: механика появляется в своём этапе. */
export function networkSystem(_state: MatchState): void {}
